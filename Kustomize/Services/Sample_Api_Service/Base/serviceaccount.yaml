apiVersion: v1
kind: ServiceAccount
metadata:
  name: sample-api-service
  annotations:
    # IRSA role granting secretsmanager:GetSecretValue on this service's
    # per-environment secret - replace with the real role per overlay via a patch,
    # or drop this annotation if the cluster grants access via node instance profile instead.
    eks.amazonaws.com/role-arn: "arn:aws:iam::951734991809:role/sample-api-service-dev-irsa"
